import { CompilerPluginHooks, TransformerEntry } from './types';
export declare function loadTsPlugins(plugins: TransformerEntry[], moduleResolver?: typeof require.resolve): {
    compilerPluginHooks: CompilerPluginHooks;
    hasPlugin: boolean;
};
