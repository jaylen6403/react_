"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.completed = exports.installingPlugins = exports.skippedInstallation = exports.initial = void 0;
const initial = () => ({
    text: "Installing packages..."
});
exports.initial = initial;
const skippedInstallation = (ctx) => ({
    text: `Skipped installing Zeplin CLI packages`,
    subtext: ctx.cliOptions.skipInstall ? "Dependencies added into package.json" : undefined
});
exports.skippedInstallation = skippedInstallation;
const installingPlugins = () => ({
    text: "Installing plugins..."
});
exports.installingPlugins = installingPlugins;
const completed = (ctx) => ({
    text: `Installed packages`,
    subtext: Object.keys(ctx.installedPackages).map(packageName => `${packageName}@${ctx.installedPackages[packageName]}`).join("\n")
});
exports.completed = completed;
