import {
  LoadingSpinner,
  LoadingSpinnerContainer,
} from '../Spinner/Spinner.style';

export const Spinner = () => {
  return (
    <LoadingSpinnerContainer>
      <LoadingSpinner />
    </LoadingSpinnerContainer>
  );
};
